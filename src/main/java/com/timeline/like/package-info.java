/**
 * 좋아요 도메인 — Phase 0에서는 경계만 잡는 빈 패키지다.
 *
 * <p>들어올 것: Like 엔티티, LikeRepository, LikeController, LikeService. 0.11에서 채운다.
 *
 * <p>경계 규칙 1 — Like 엔티티와 LikeRepository는 이 패키지 밖으로 노출하지 않는다.
 */
package com.timeline.like;
