# 대규모 트래픽 대응 타임라인 서비스

> Spring Boot 기반 백엔드 전용 프로젝트 · 이직 포트폴리오

---

## 1. 프로젝트 개요

트위터(X) 형태의 **팔로우 기반 타임라인 서비스**를 백엔드 API로만 구현한다.

기능 자체는 단순하다. 팔로우한 사용자의 게시글이 최신순으로 내려오는 것이 전부다.
이 프로젝트의 목적은 **기능 구현이 아니라 성능 개선 과정을 증명하는 것**이다.

동일한 기능을 3단계(Pull → Push → Hybrid)로 재구현하면서 각 단계의 한계를 수치로 측정하고,
왜 다음 단계로 넘어가야 했는지를 근거와 함께 기록한다.

**증명한다는 것은 측정이 신뢰할 수 있다는 뜻이다.**
따라서 이 문서에서 §9(측정 계획)는 부록이 아니라 §7(설계 쟁점)과 동급의 본문이다.
측정 방법이 흔들리면 개선 폭도, 그것을 설명하는 서사도 전부 무너진다.

### 이 프로젝트가 답해야 할 질문

- 팔로잉 500명인 사용자의 타임라인을 어떻게 200ms 안에 응답하는가
- 팔로워가 아주 많은 계정이 글을 쓰면 무슨 일이 벌어지는가
- 캐시와 DB의 정합성은 어떻게 유지하며, **그것을 어떻게 검증하는가**
- Redis가 죽어도 서비스가 살아있게 하려면 어떻게 설계해야 하는가
  (→ 답은 "Pull 폴백"만으로는 부족하다. §7.13)

### 산출물

| 항목 | 내용 |
|---|---|
| GitHub 저장소 | 소스 + README(핵심 산출물) |
| 성능 리포트 | 단계별 before/after 지표 + **원시 k6 결과** |
| 아키텍처 문서 | 설계 결정과 트레이드오프 기록 |
| 재현 스크립트 | 더미 생성 · 부하 테스트 · 리포트 생성을 한 명령으로 |
| API 문서 | Swagger 자동 생성 |

---

## 2. 목표

### 측정 환경 (실사양)

```
Apple M1 · 8코어 · RAM 8GB · 디스크 여유 55GB
```

**이 8GB가 계획 전체를 지배하는 제약이다.** MySQL·Redis·Kafka·모니터링·JVM·부하 생성기를
동시에 올릴 수 없으므로, 데이터 규모(§2)와 Redis 정책(§5)과 실행 구성(§3)이 여기서 결정된다.
제약을 숨기지 않고 리포트에 명시하는 것이 수치의 신뢰도를 만든다.

### 정량 목표

| 지표 | 목표 |
|---|---|
| 타임라인 조회 p99 | **warm 캐시 · `heavy` 코호트 · 목표 RPS 주입 상태에서 200ms 이하** |
| 타임라인 조회 처리량 | **p99 200ms를 유지하며 처리 가능한 최대 RPS 1,000 이상** |
| Pull/Push 결과 불일치율 | **0%** (§9.4 동등성 검증) |
| 더미 데이터 규모 | 사용자 10만 명 / 팔로우 관계 300만 건 / 게시글 300만 건 |
| 테스트 커버리지 | 핵심 도메인 70% 이상 |

> "TPS 1,000"은 조건 없이는 검증 불가능한 문장이다.
> 무엇을 주입했고, 캐시가 어떤 상태이며, 어떤 사용자를 대상으로 했는지가 빠지면
> 숫자는 아무것도 말하지 않는다.

> 목표 수치는 로컬 환경 기준. 절대값보다 **개선 폭**과 **원인 분석**이 중요하다.

### 정성 목표

- 트레이드오프가 있는 설계 결정을 내리고 근거를 문서화하는 경험
- 실행 계획(EXPLAIN ANALYZE)을 읽고 인덱스를 직접 설계하는 능력
- 부하 테스트로 병목을 찾고 개선을 검증하는 사이클 체득
- **측정 자체를 설계하는 능력** — 무엇을 고정하고 무엇을 변화시킬 것인가

---

## 3. 기술 스택

### 확정

| 영역 | 선택 | 이유 |
|---|---|---|
| Language | Java 21 | Virtual Thread 실험 (fan-out 워커 한정, → §8 Phase 2b) |
| Framework | Spring Boot 3.x | |
| ORM | Spring Data JPA + `@Query` | 쿼리가 전부 정적이라 동적 쿼리 도구가 불필요 |
| Migration | **Flyway** | 300만 건 적재 상태에서 인덱스를 반복 변경한다 |
| RDB | MySQL 8.0 | |
| Cache | Redis 7 | 타임라인 저장소 |
| Message Queue | Kafka (KRaft) | Phase 3에서 도입 |
| Test | JUnit5, Testcontainers | 실제 MySQL/Redis로 통합 테스트 |
| Load Test | k6 | 스크립트가 JS라 진입장벽 낮음 |
| Monitoring | Actuator + Micrometer + Prometheus + Grafana | |
| Profiling | **MySQL slow log + `EXPLAIN ANALYZE` + JFR** | 병목의 위치를 실제로 짚기 위해 |
| Infra | Docker Compose (오버레이 3종) | |
| CI | GitHub Actions | |

**Flyway를 넣은 이유**는 데이터 보호만이 아니다.
인덱스를 추가하고 되돌린 이력이 마이그레이션 파일로 남으면, **그 자체가 실패한 시도의 기록**이 된다(§10).

**프로파일링 도구가 필요한 이유**: Phase 1의 기록 항목은 "병목이 어디인가(JOIN? 정렬? I/O?)"인데,
Prometheus/Grafana만으로는 이 질문에 답할 수 없다.
`EXPLAIN`은 옵티마이저의 추정치이고 `EXPLAIN ANALYZE`는 실측이다 — 둘의 차이 자체가 분석거리다.

### 실행 구성 — 8GB에 맞춘 3개 프로파일

```
compose.yml              # dev
compose.bench.yml        # + 모니터링
compose.async.yml        # + Kafka, 모니터링 제외
```

| 프로파일 | 구성 | Docker 메모리 |
|---|---|---|
| `dev` (평소 개발) | MySQL 2G + Redis 1.5G | 3.5G |
| `bench` (측정 시) | dev + Prometheus 0.4G + Grafana 0.3G | 4.2G |
| `async` (Phase 3) | MySQL 2G + Redis 1.5G + Kafka 1.2G, **모니터링 off** | 4.7G |

- Docker Desktop 할당: **CPU 5 / 메모리 5GB**
- 애플리케이션은 호스트에서 직접 실행 (`-Xmx1g`)
- k6도 호스트 네이티브 실행

**모니터링을 상시 구동하지 않는다.** Phase 3에서 Kafka 자리를 만들려면 내리는 수밖에 없고,
그때 fan-out 지연은 Actuator와 애플리케이션 로그로 계측한다.

### 의도적으로 제외

- **프론트엔드** — API 설계와 성능에 집중
- **MSA** — 혼자 하는 프로젝트에서 서비스 분리는 오버엔지니어링. 모듈 경계만 명확히 나눈 모놀리식으로 간다
- **Kubernetes** — 학습 비용 대비 얻는 게 적다
- **소셜 로그인** — 인증은 JWT 자체 구현으로 충분
- **QueryDSL** — 이 서비스의 쿼리는 전부 정적이다. 커서 조건 하나가 동적 요소의 전부라
  도입해도 "왜 썼는지"를 설명할 수 없다
- **게시글 수정 API** — 성능 서사에 기여하지 않는다. 삭제만으로 캐시 정합성 논의가 성립한다
- **댓글 · 해시태그 · 검색 · DM · 차단** — 성능 외 기능은 늘리지 않는다
- **Caffeine 2계층 캐시** — §7.10. 로컬 캐시 간 정합성까지 떠안으면 초점이 흐려진다

> **제외한 이유를 README에 적는 것도 포트폴리오다.** 다 넣는 것보다 안 넣은 판단이 더 성숙해 보인다.

---

## 4. 도메인 모델

### 4.1 ERD

```
users
├── id                BIGINT PK
├── username          VARCHAR(50) UNIQUE
├── password          VARCHAR(255)
├── nickname          VARCHAR(50)
├── follower_count    INT DEFAULT 0          -- 비정규화 카운터 (self-follow 제외)
├── is_influencer     BOOLEAN DEFAULT false  -- Hybrid 분기 플래그 (단방향 승격)
├── influencer_since  DATETIME NULL          -- 승격 시각. 머지 범위의 경계
└── created_at        DATETIME

follows
├── id            BIGINT PK
├── follower_id   BIGINT FK → users
├── followee_id   BIGINT FK → users
└── created_at    DATETIME
    UNIQUE (follower_id, followee_id)  -- Pull 조회 진입 인덱스를 겸한다
    INDEX (followee_id, follower_id)   -- fan-out 대상 조회용 (커버링)
    ※ 가입 시 self-follow 행(follower_id = followee_id)을 1건 삽입한다

posts
├── id            BIGINT PK           -- Snowflake ID
├── author_id     BIGINT FK → users
├── content       VARCHAR(500)        -- 더미 평균 80B
├── like_count    INT DEFAULT 0       -- 비정규화 카운터
├── is_deleted    BOOLEAN DEFAULT false  -- soft delete
└── created_at    DATETIME
    INDEX (author_id, id DESC)        -- Pull 방식 조회용 (최종 상태)
    ※ 조회용 보조 인덱스는 V1 마이그레이션에 넣지 않는다.
       인덱스는 M0(인덱스 없음) 측정과 EXPLAIN ANALYZE 분석의 산출물로
       Phase 1에서 최초 도입한다 (§9.2 — M1이 "인덱스만으로는 한계"의 증거가 되려면).
       (author_id, is_deleted, id DESC)는 후보로 두고 실측 비교 후 채택한다

likes
├── id            BIGINT PK
├── post_id       BIGINT FK → posts
├── user_id       BIGINT FK → users
└── created_at    DATETIME
    UNIQUE (post_id, user_id)
```

### 4.2 왜 Snowflake ID인가

`posts.id`를 AUTO_INCREMENT가 아닌 Snowflake로 생성한다.

```
64bit = [1bit 미사용][41bit 타임스탬프][10bit 노드ID][12bit 시퀀스]
```

- ID 자체에 시간 정보가 들어있어 **정렬 = 시간순 정렬**
- Redis Sorted Set의 score로 ID를 그대로 쓸 수 있다
- 커서 페이지네이션의 커서로 ID 하나만 쓰면 된다 (타임스탬프+ID 복합 커서 불필요)
- DB 의존 없이 애플리케이션에서 생성 → 삽입 병목 제거

**더미 데이터를 만들 때 이 성질이 함정이 된다.**
상위 41bit가 생성 시각이므로, 지금 300만 건을 만들면 전부 "오늘 몇 분 사이"의 ID가 된다.
`created_at`만 과거로 흩뿌리면 **ID 순서와 시간 순서가 어긋나 위 전제가 더미에서 깨진다.**

→ 프로덕션 생성기는 `nextId()`만 노출하고,
   **타임스탬프 주입형 팩토리 `of(epochMilli, nodeId, sequence)`를 더미 생성 소스셋에만 둔다.**
   프로덕션 코드에 백데이팅 API를 노출하면 그것이 곧 ID 위조 경로다.

### 4.3 왜 self-follow 행을 넣는가

Pull 쿼리는 `JOIN follows ON p.author_id = f.followee_id WHERE f.follower_id = :userId`이므로
**내가 쓴 글이 결과에 들어오지 않는다.** 반면 Push 방식은 본인 타임라인에 직접 ZADD한다(§7.7).

그대로 두면 **Phase 1과 Phase 2가 서로 다른 데이터를 반환하는데 p99를 비교하게 된다.**
사과와 오렌지를 나란히 놓고 "94% 개선"이라 쓰는 셈이다.

가입 시 self-follow 행을 1건 넣으면 두 경로의 결과 집합이 같아진다.

- 대가: 팔로워/팔로잉 목록 API와 `follower_count`에서 `follower_id != followee_id` 조건이 필요하다 (각 한 줄)
- 버린 대안: Pull 쿼리에 `UNION ALL`로 자기 글을 합치는 방법
  → **UNION이 임시 테이블을 만들어 Phase 1의 병목 분석을 오염시킨다.**
    실행 계획 분석은 이 프로젝트의 핵심 산출물이므로 쿼리를 단일 JOIN으로 유지하는 편이 낫다

### 4.4 카운터 비정규화

`like_count`, `follower_count`를 컬럼으로 들고 간다.
매 조회마다 `COUNT(*)`를 날리면 게시글 20개 조회에 COUNT 쿼리 20번이 추가된다.

대신 정합성 문제가 생긴다. 동시에 좋아요가 눌리면 갱신이 유실될 수 있다.

**결정: 카운터의 진실은 DB다** (근거는 §7.4)

```sql
-- likes 행 삽입과 같은 트랜잭션에서 원자적 증감
UPDATE posts SET like_count = like_count + 1 WHERE id = ?
```

- 읽기는 `post:{postId}` 캐시(TTL 1시간)를 경유 → **최대 1시간 stale을 허용한다**
- 좋아요를 누른 본인에게는 `DEL post:{postId}` 후 갱신값을 직접 반환 → 즉시 반영
- **Redis 카운터(`like:count:{postId}`)는 쓰지 않는다**

---

## 5. Redis 키 설계

| 키 | 자료구조 | 값 | TTL | 용도 |
|---|---|---|---|---|
| `timeline:{userId}` | Sorted Set | score=postId, member=postId | 7일 | 사용자별 타임라인 |
| `post:{postId}` | String (JSON) | 게시글 본문 | 1시간 | 게시글 캐시 (전역 공유) |
| `post:{postId}` | String | tombstone | 60초 | **없는/삭제된 글의 negative cache** |
| `active:users` | Sorted Set | score=최근접속시각 | - | fan-out 대상 필터링 |
| `timeline:lock:{userId}` | String | `1` (SET NX) | 10초 | 중복 폴백 방지 (§7.13) |
| `refresh:{userId}` | String | Refresh Token | 14일 | 토큰 재발급 |

**설계에서 뺀 키 2개**

- `followers:{userId}` — §5-6 참조. 캐시하지 않고 DB에서 청크 조회한다
- `like:count:{postId}` — §4.4. 카운터의 진실을 DB로 정했으므로 불필요하다.
  게시글 300만 건에 대해 TTL 없는 키를 만들면 1GB Redis에서 타임라인이 쓸 메모리를 잠식한다

### 핵심 원칙

**1. 타임라인에는 post_id만 저장한다**

본문을 복사하면 안 된다.
- 메모리: 본문 × 팔로워 수만큼 중복된다. ID만 쓰면 엔트리 하나로 끝난다
- 정합성: **soft delete 플래그 하나만 바꾸면 흩어진 모든 타임라인에 즉시 반영된다.**
  본문을 복사했다면 삭제 표시를 팔로워 수만큼 고쳐야 한다

> 원래 이 자리에는 "글 수정 시 복사본을 다 고쳐야 한다"는 논거가 있었으나,
> 수정 API를 구현하지 않기로 했으므로(§3) 실재하지 않는 기능에 기댄 논거였다.
> 삭제는 실제로 구현하므로 같은 논리가 **실증 가능한 형태로** 성립한다.

**2. 조회는 2단계**

```
1) ZREVRANGEBYSCORE timeline:1001 (cursor -inf LIMIT 0 25
   → post_id 목록

2) MGET post:9982 post:9975 ...
   → 본문 (캐시 미스는 DB 조회 후 채움, 부재는 tombstone으로 채움)
```

25개를 꺼내서 삭제된 글을 걸러낸 뒤 20개를 반환한다. (재조회 상한은 §7.5)

**3. Sorted Set을 쓰는 이유는 두 가지다**

커서 페이지네이션이 표면적 이유라면, **멱등성이 더 본질적인 이유**다.

```
ZADD timeline:1001 9982 9982   -- 몇 번을 실행해도 결과가 같다 (멱등)
LPUSH timeline:1001 9982       -- 실행할 때마다 하나씩 늘어난다 (비멱등)
```

fan-out 중 서버가 죽어 재시도가 발생해도 중복이 생기지 않는다.
List를 썼다면 재시도가 곧 데이터 오염이다. (→ §7.3)

이 선택은 §7.11의 Kafka 파티션 키 결정까지 규정한다.
**score 기반 정렬이므로 메시지 도착 순서가 결과에 영향을 주지 않기 때문이다.**

**4. 타임라인 길이 제한 — 500개**

```
ZADD timeline:{userId} {postId} {postId}
ZREMRANGEBYRANK timeline:{userId} 0 -501
```

최근 500개만 유지. 그 이상 스크롤하면 DB Pull로 폴백한다.

- 500개 = 20개씩 25페이지. 실사용에서 그 이상 스크롤하는 경우는 드물다
- 로컬 Redis를 1GB로 제한하므로(§5-5) 500개 쪽이 유지 가능한 사용자 수에 여유가 있다
- **엔트리 크기는 추산하지 않고 `MEMORY USAGE`로 실측해 리포트에 기록한다.**
  500개는 `zset-max-listpack-entries`(기본 128)를 넘어 skiplist로 전환되므로
  흔히 쓰는 80B 추산은 낙관적이다

README에는 실측값을 근거로 실서비스 스케일 계산을 별도 제시한다.
**"로컬 제약 때문에 500으로 줄였고, 실서비스라면 800이 적절하다"**를 함께 적는다.

전체 가입자에게 fan-out하면 감당이 안 된다. **최근 7일 내 접속자에게만 fan-out**한다.

**5. Redis 메모리가 차면 — 그 자체가 실험이다**

```
maxmemory 1gb
maxmemory-policy allkeys-lru
```

메모리가 차면 오래 안 쓴 타임라인부터 축출되고, 축출된 사용자는 Pull 폴백으로 자연 복구된다.
→ §11의 "Redis 메모리가 부족해지면?"에 **추측이 아니라 실측으로 답할 수 있게 된다.**

**6. fan-out 대상은 캐시하지 않는다**

팔로워 목록을 Redis에 캐싱하지 않고 DB에서 청크 단위로 직접 읽는다.

- 인덱스 `(followee_id, follower_id)`가 커버링 인덱스라 레인지 스캔 한 번이면 된다
- 캐시를 두면 무효화가 필요하다. TTL만 걸어두면
  **새 팔로워가 최대 TTL 시간 동안 글을 못 받는 실질적 데이터 유실이 발생한다**
  (§7.9의 백필로도 이 구간은 메워지지 않는다)
- **fan-out은 비동기 경로다.** 수십 ms의 DB 조회는 문제가 되지 않는다 — 읽기 경로가 아니다
- 캐시를 하나 줄이면 정합성 사고 지점이 하나 줄어든다

활성 사용자 필터링은 청크(500명)마다 `ZSCORE active:users {followerId}`를
**파이프라인 1회**로 묶어 처리한다. 인플루언서를 Push에서 제외했으므로 팔로워 최대치가
임계치(5,000)이고, 청크는 최대 10개다.
`ZINTERSTORE`는 임시 키 정리 실패 시 메모리가 새고, Lua는 이 규모에서 이득 없이 디버깅만 어렵다.

**7. Redis는 언제든 죽을 수 있다 — 그런데 Pull 폴백만으로는 부족하다**

```
timeline:{userId} 조회 → 키 없음
  → DB Pull 쿼리로 직접 조회
  → 결과를 Redis에 채움
  → 반환
```

Push 방식을 쓰더라도 **Pull 경로는 반드시 살려둔다.**

**다만 이것은 개별 캐시 미스의 답이지 Redis 전면 장애의 답이 아니다.**
Redis가 죽으면 전 사용자가 동시에 이 경로로 몰린다 — Phase 1에서 가장 느렸던 바로 그 쿼리로.
→ §7.13에서 다룬다.

---

## 6. API 설계

Base: `/api/v1`

### 인증
```
POST   /auth/signup
POST   /auth/login          → JWT 발급
POST   /auth/reissue        → Refresh Token (refresh:{userId})
```

### 사용자 · 팔로우
```
GET    /users/{userId}
POST   /users/{userId}/follow
DELETE /users/{userId}/follow
GET    /users/{userId}/followers?cursor=&size=     -- self-follow 행 제외
GET    /users/{userId}/followings?cursor=&size=    -- self-follow 행 제외
```

### 게시글
```
POST   /posts
GET    /posts/{postId}
DELETE /posts/{postId}          -- soft delete
POST   /posts/{postId}/likes
DELETE /posts/{postId}/likes
```

> 수정(`PATCH /posts/{postId}`)은 구현하지 않는다. → §3 의도적 제외

### 타임라인 (핵심)
```
GET    /timeline?cursor={postId}&size=20
GET    /users/{userId}/posts?cursor={postId}&size=20
```

### 커서 페이지네이션 응답 형식

```json
{
  "data": [ ... ],
  "nextCursor": 9877123456789,
  "hasNext": true
}
```

**과부하 시 응답** — Pull 폴백이 포화되면 부분 결과를 만들어내지 않고 명시적으로 거절한다.

```
503 Service Unavailable
Retry-After: 1
```

### OFFSET을 쓰지 않는 이유

```sql
-- 100만 번째 페이지
SELECT * FROM posts ORDER BY id DESC LIMIT 20 OFFSET 1000000;
-- → 100만 20건을 읽고 100만 건을 버린다
```

커서 방식은 인덱스를 타고 바로 진입한다.

```sql
SELECT * FROM posts WHERE id < :cursor ORDER BY id DESC LIMIT 20;
```

---

## 7. 핵심 설계 쟁점

이 프로젝트의 실질적 가치는 기능이 아니라 **아래 질문들에 대한 답과 근거**에 있다.
각 쟁점은 **결정 → 근거 → 버린 대안 → 검증 방법** 순으로 기록한다.
결정만 있고 근거가 없으면 면접에서 대화가 이어지지 않고,
검증 방법이 없으면 근거가 주장에 머문다.

---

### 7.1 Push가 항상 옳은가 — 읽기/쓰기 비율

Push의 근거는 "피드는 읽기가 많으니까"인데, 이는 조건부 참이다.

```
Push 비용 ∝ 글 작성 수 × 평균 팔로워 수
Pull 비용 ∝ 타임라인 조회 수 × 평균 팔로잉 수
```

**결정: 읽기:쓰기 = 50:1을 가정한다.**
k6 혼합 시나리오는 조회 98% : 작성 2%로 구성하고,
**fan-out 지연은 이 시나리오가 아니라 별도 단독 시나리오에서 측정한다.**

**근거**

DDIA가 인용한 트위터 실측은 홈 타임라인 조회 300k req/s 대 트윗 작성 4.6k req/s로 약 65:1이다.
**50:1은 그보다 보수적으로 잡은 값이다.**
읽기 비율을 높게 잡을수록 Push에 유리해지므로, 자기 주장에 불리한 쪽으로 가정해야 결론이 방어된다.

**그런데 비율만으로는 답이 안 나온다.**

```
총비용 = R × C_pull(평균 팔로잉) + W × C_push(평균 팔로워)
```

우리 데이터셋에서 평균 팔로잉 = 평균 팔로워 = 30(= 300만 / 10만)으로 **같다.**
그런데도 Push가 이긴다. `C_pull`은 디스크 접근과 정렬 비용이고 `C_push`는 메모리 삽입 비용이라
**단위 비용이 자릿수 단위로 다르기 때문이다.**

→ **Push를 뒤집는 것은 읽기:쓰기 비율이 아니라 분포의 꼬리다.**
   인플루언서 한 명이 `C_push`를 수천 배로 키운다. 그래서 Hybrid가 필요하다.
   이 논리는 §10의 "이 문제의 일반화"(핵심 난제는 fan-out이 아니라 분포의 쏠림)와 그대로 이어진다.

**검증**: 혼합 시나리오(조회 98/작성 2)와 작성 편중 시나리오를 각각 돌려
읽기 비용과 쓰기 비용의 절대값을 따로 기록한다.

---

### 7.2 Hybrid 임계치를 팔로워 수만으로 정하는 게 맞는가

실제 부하는 팔로워 수가 아니라 다음 값이다.

```
fan-out 총량 = 팔로워 수 × 글 작성 빈도
```

| 계정 | 팔로워 | 작성 빈도 | 실제 부하 |
|---|---|---|---|
| A | 아주 많음 | 월 1회 | 거의 없음 |
| B | 중간 | 일 50회 | 매우 큼 |

**B가 훨씬 위험한데 팔로워 기준으로는 걸러지지 않는다.**

또한 임계치는 양방향으로 비용을 옮길 뿐이다.

```
임계치 ↓  → 인플루언서 증가 → 읽기 시점 머지 비용 ↑
임계치 ↑  → Push 대상 증가  → 쓰기 지연 ↑
```

**결정 1: 임계치 = 팔로워 5,000. 비교 후보는 500 / 5,000 / 20,000.**

| 임계치 | 인플루언서 수 | Push 대상 관계 수 | 읽기 머지 대상 |
|---|---|---|---|
| 500 | 1,110명 | 180만 | 많음 → 읽기 비용 ↑ |
| **5,000** | **110명** | **230만** | **적당** |
| 20,000 | 10명 | 280만 | 거의 없음 → Hybrid 효과 미미 |

비교 하한이 1,000이 아니라 500인 이유: 더미 분포에서 B 계층의 팔로워가 정확히 500이라,
임계치 1,000의 승격 집합은 S+A(110명)로 **5,000과 완전히 같아져 비교가 성립하지 않는다.**
하한은 B 계층이 걸리는 값이어야 세 측정 조건이 실제로 달라진다.

원안의 1만/10만/50만은 사용자가 10만 명인 데이터셋에서 성립하지 않는다.
팔로워 10만은 "전원이 팔로우"를 뜻하기 때문이다(§8 Phase 1의 분포 참조).
비율로 보면 스케일 다운이 타당하다 — 팔로워 20,000은 전체 사용자의 20%로,
트위터 최상위 계정이 차지하는 비율과 크게 다르지 않다.

**5,000을 1차값으로 택한 이유는 머지 경로가 실제로 발동해야 Hybrid를 측정할 수 있기 때문이다.**
20,000이면 인플루언서가 10명뿐이라 일반 사용자가 한 명도 팔로우하지 않을 수 있다.

**결정 2: `is_influencer`는 단방향 승격이며, `influencer_since`로 경계를 기록한다.**

- 팔로우 발생 시 `follower_count`가 임계치를 넘으면 승격하고 `influencer_since = now()`
- **강등하지 않는다**
- 읽기 시 머지는 `postId > snowflakeOf(influencer_since)` 범위로 한정

승격 시각을 기록하면 경계가 명확해진다.
그 이전 글은 이미 Push되어 타임라인에 있고, 이후 글만 머지 대상이므로
**중복도 누락도 발생하지 않는다.**

강등을 지원하면 "인플루언서 기간에 쓴 글을 전 팔로워에게 소급 백필"이라는 문제가 새로 생긴다.
팔로워 수는 실서비스에서 거의 단조 증가하므로 강등의 실익이 없고,
단방향으로 두면 임계치 근처 진동(flapping)을 막기 위한 히스테리시스 설계도 불필요해진다.

**남는 한계 (README에 명시)**
작성 빈도를 보지 않는다. 팔로워 5,000명이 하루 50번 쓰는 계정은
팔로워 20,000명이 월 1번 쓰는 계정보다 위험한데 이 설계로는 걸러지지 않는다.

**검증**: 임계치 3종에 대해 읽기 머지 비용과 쓰기 지연을 각각 측정해 양방향 트레이드오프를 수치로 제시.

---

### 7.3 fan-out 도중 실패하면 — 멱등성

팔로워 5,000명 중 3,000명까지 처리하고 서버가 죽었다. 재시도하면 앞의 3,000명은 중복이다.

여기서 **Sorted Set을 택한 진짜 이유**가 드러난다.

```
ZADD timeline:1001 9982 9982   -- 몇 번을 실행해도 결과가 같다 (멱등)
LPUSH timeline:1001 9982       -- 실행할 때마다 하나씩 늘어난다 (비멱등)
```

List를 썼다면 재시도가 곧 중복이다.
연산이 멱등이므로 **Kafka의 at-least-once 전달로 충분하다**는 논리가 성립한다.

**이 선택은 §7.11의 파티션 키 결정까지 규정한다.**
Sorted Set은 score(postId)로 정렬하므로 메시지 도착 순서가 결과에 영향을 주지 않고,
그 결과 순서 보장을 포기할 수 있어 파티션을 균등하게 쓸 수 있다.

**검증**
- fan-out을 인위적으로 중단한 뒤 재처리하고, 결과가 정상 처리와 동일한지 확인
- 재처리 후 Pull 결과와의 동등성 검증(§9.4)이 통과하는지 확인

---

### 7.4 무엇이 진실(Source of Truth)인가

이 구분이 흐릿하면 장애 복구 전략이 나오지 않는다.

| 데이터 | 진실 | 근거 |
|---|---|---|
| 타임라인 캐시 | **DB** | 파생 데이터. 유실돼도 Pull로 재구축 가능 |
| 게시글 본문 | **DB** | 캐시는 사본 |
| 좋아요 카운터 | **DB** | 아래 참조 |

**결정: 좋아요 카운터의 진실도 DB다. Redis 카운팅을 하지 않는다.**

- 쓰기: `likes` 행 삽입 + `UPDATE posts SET like_count = like_count + 1`을 한 트랜잭션
- 읽기: `post:{postId}` 캐시 경유 → **타인에게는 최대 1시간 stale하게 보인다**
- 본인에게는 `DEL post:{postId}` 후 갱신값 반환 → 즉시 반영

**근거**

1. **좋아요는 타임라인 조회 대비 발생 빈도가 낮다.** 쓰기 경로를 최적화해 얻는 실익이 작다
2. Redis 카운팅 + 배치 동기화는 배치 직전 장애 시 증가분이 사라진다.
   그러면 "무엇이 진실인가"에 답할 수 없다 — 이 절이 던진 질문에 스스로 답하지 못하게 된다
3. 게시글 300만 건에 대해 TTL 없는 카운터 키를 만들면
   1GB Redis에서 타임라인이 쓸 메모리를 잠식한다
4. **이 프로젝트의 병목은 타임라인 조회지 좋아요가 아니다.**
   여기에 복잡도를 쓰면 초점이 흐려진다(§12)

> **명시적으로 허용하는 부정확성**: 타인에게 최대 1시간 stale.
> 근사값을 허용한 것이 아니라, **정확한 값을 DB에 두고 캐시 신선도만 양보했다.**
> 어느 쪽이든 명시적으로 결정하고 이유를 기록하는 것이 이 절의 목적이다.

---

### 7.5 Lazy 삭제 필터링의 최악 케이스

읽을 때 걸러내는 방식은 평상시엔 문제없다. 그러나:

> 팔로우한 사용자가 자기 글 500개를 한꺼번에 삭제

25개를 꺼냈는데 20개가 삭제된 글이면 5개만 남는다.
재조회를 반복하다 **최악의 경우 타임라인 전체를 훑고도 20개를 못 채운다.**

**결정: 재조회 상한 3회. 부족해도 있는 만큼 반환하고 `hasNext=true`로 응답한다.**

**근거 — 숫자로 댈 수 있다**

25개씩 3회 = 75개 스캔. 삭제율 `p`일 때 유효 건수는 `75(1−p)`.

```
75(1−p) ≥ 20  →  p ≤ 0.73
```

**삭제율 73%까지는 3회로 20개가 채워진다.**
클라이언트는 `nextCursor`로 계속 요청할 수 있으므로 그 이상에서도 치명적이지 않다.

**버린 대안**
- 백그라운드 타임라인 재구축 — 트리거 임계치를 또 정해야 하고 구현 비용 대비 서술 가치가 낮다
- 대량 삭제 시 eager 제거 — §7.8에서 A안을 버리는 이유와 같다(500개 스캔 × 팔로워 수)

**함께 필요한 것**: negative caching(§5).
없으면 삭제된 글마다 DB를 쳐서 이 시나리오가 그대로 DB 부하로 증폭된다.

**검증**: 삭제율을 0/30/50/70/90%로 바꿔가며 응답 건수와 지연을 측정.

---

### 7.6 비활성 사용자가 돌아오면

활성 사용자에게만 fan-out하므로, 오랜만에 돌아온 사용자의 타임라인은 비어 있다.
로그인 시점에 Pull로 재구축해야 하는데 — **그것이 정확히 우리가 피하려던 무거운 쿼리다.**

**결정: 별도 기능을 만들지 않는다. §7.13의 폴백 경로에 흡수한다.**

타임라인 조회 시 캐시가 비어 있으면 폴백이 최근 100개만 Pull해 캐시에 채운다.
로그인 시 사전 워밍이나 비동기 재구축을 하지 않는다.

**근거**

- **비활성 복귀는 캐시 미스의 특수 케이스일 뿐이다.**
  이미 있는 폴백을 재사용하면 구현 비용이 0이고, 보호 장치(세마포어·락)도 함께 적용된다
- 100개로 제한하는 이유: Pull 비용이 LIMIT에 비례한다.
  500개 전체 재구축은 무겁고, 사용자가 실제로 보는 것은 첫 페이지다
- "불러오는 중" 상태를 만들면 API 계약이 복잡해진다.
  백엔드 전용 프로젝트라 클라이언트와 협의할 수도 없다
- 로그인 이벤트 사전 워밍은 별도 인프라가 필요한 오버엔지니어링이다

> 쟁점을 하나 없앤 것이 아니라, **이미 만든 메커니즘으로 답이 된다는 것을 확인한 것이다.**
> 설계가 맞물려 있다는 증거이므로 README에는 남긴다.

---

### 7.7 [필수] 내가 쓴 글이 즉시 보이는가 — Read-Your-Own-Write

Kafka로 fan-out을 비동기화하면 수 초 지연이 생긴다.
남의 글이 늦는 건 아무도 모르지만, **내 글이 안 보이면 사용자는 "안 올라갔다"고 판단하고 다시 쓴다.**

```
글 작성 요청
  ├─ 본인 타임라인에 ZADD          (동기, 즉시)
  └─ 나머지 팔로워 fan-out 이벤트   (비동기)
  → 응답
```

분산 시스템의 고전 문제인 **read-your-own-write consistency**다.
구현은 몇 줄이지만, 이 문제를 예상했다는 것 자체가 실사용을 상상해본 사람이라는 신호다.

**결정**: 본인 타임라인만 동기 ZADD. self-follow 행이 있으므로
fan-out 대상 목록에서 본인을 한 줄로 제외한다(이미 처리했으므로).

**검증**: 비동기 지연을 인위적으로 5초 주입한 상태에서 작성 직후 본인 글이 즉시 보이는지 테스트.

---

### 7.8 [필수] 언팔로우하면 이미 들어간 글은?

| 방식 | 비용 | 문제 |
|---|---|---|
| A. 즉시 제거 | 내 타임라인 500개 훑어 해당 작성자 글 ZREM | 팔로잉이 많으면 비쌈 |
| B. 읽기 시 필터 | 매 조회마다 팔로잉 목록 대조 | 읽기 경로에 부하 추가 |
| C. 방치 | 0 | 언팔했는데 글이 계속 보임 |

**결정: C(방치). 단 A/B/C의 비용을 실측한 표를 README에 남긴다.**

**근거**

- **B가 가장 먼저 탈락한다.** 매 조회마다 팔로잉 목록(최대 500)을 대조하는데,
  **이 프로젝트의 목적이 읽기 최적화인 상황에서 정반대 방향이다**
- A는 언팔 1회에 타임라인 500개를 훑고 작성자를 조회해 ZREM한다.
  언팔은 드문 이벤트지만 비용이 크고 비동기여야 한다
- C는 비용이 0이고, 타임라인 상한 500이 있어 **새 글이 안 들어오면 며칠 내 자연 소멸한다.**
  사용자는 25페이지까지 스크롤하지 않으므로 인지하기도 어렵다

**"제품 관점에서 허용 가능한 부정확성"** 이라는 판단이 들어가는 지점으로,
엔지니어링과 제품 감각을 동시에 보여줄 수 있다.

> **단서 — 이 판단이 언제 뒤집히는가**
> 차단(block)이었다면 C를 택할 수 없다.
> 언팔로우는 "관심 없음"이지만 차단은 "보이면 안 됨"이라 **허용 가능한 부정확성의 범위가 다르다.**
> 이 프로젝트에 차단 기능이 없기 때문에 C가 성립한다.

---

### 7.9 [권장] 새로 팔로우하면 과거 글이 소급되는가

팔로우 시점에 해당 사용자의 최근 N개를 내 타임라인에 ZADD하면 된다.
그러나 **인플루언서를 팔로우하면 내 타임라인이 그 사람 글로 도배**된다.

**결정: N = 20. 비동기 처리. 인플루언서는 백필하지 않는다.**

**근거**

- **N=20인 이유: 타임라인 첫 페이지가 정확히 20개다.**
  "팔로우 직후 첫 페이지에 그 사람 글이 보인다"가 사용자가 체감하는 전부이고,
  그 이상은 스크롤해야 보이는데 그때는 이미 새 글이 쌓여 있다
- **인플루언서 제외가 도배 문제를 자동으로 해결한다.**
  인플루언서는 애초에 Push 대상이 아니라 읽기 시 머지되므로 백필이 불필요하다
  → **Hybrid의 부수 효과로 이 쟁점의 절반이 사라진다.** 설계가 맞물린 증거다
- 비동기인 이유: §7.7과 달리 즉시성 요구가 약하다.
  Phase 2b는 `@Async`, Phase 3는 Kafka 이벤트로 처리한다

---

### 7.10 [제외] 핫 게시글의 캐시 쏠림

`post:{postId}`는 전역 공유라, 바이럴 글 하나에 **Redis 단일 키로 초당 수만 요청**이 몰린다.
Redis 클러스터를 써도 그 키는 한 노드에만 있으므로 그 노드만 죽는다.

**결정: 2계층 캐시(Caffeine)를 구현하지 않는다. README에 문제와 대응 방향만 적는다.**

로컬 캐시 간 정합성 문제까지 떠안으면 초점이 흐려지고, 일정에도 여유가 없다(§8).

**다만 비용이 거의 없는 두 가지는 한다** (§7.13에 포함)
- negative caching — 부재/삭제 글의 tombstone
- 동일 키에 대한 중복 폴백 방지 — `SET NX` 락

---

### 7.11 Kafka 파티션 키

- `author_id`로 파티셔닝 → 인플루언서가 속한 파티션만 밀림 (핫 파티션)
- 랜덤 분산 → 같은 사용자 글의 순서 역전 가능

**결정: 파티션 키를 지정하지 않는다(라운드로빈). 팔로워를 500명 단위 청크로 분할해 발행한다.**

**근거 — §7.3의 선택이 여기까지 규정한다**

1. **ZADD는 score(postId) 기준으로 정렬하므로 메시지 도착 순서가 결과에 영향을 주지 않는다.**
   순서 보장을 포기해도 잃는 것이 없다
2. 순서 제약이 없으므로 파티션을 균등하게 쓸 수 있다 → **핫 파티션이 원천 차단된다**
3. soft delete를 쓰므로 ZREM 이벤트가 없다. ZADD/ZREM 순서 역전 문제도 생기지 않는다
4. Hybrid로 팔로워 5,000 이상을 Push에서 제외했으므로 최대 청크 수는 10개다

> **"Sorted Set을 택했기 때문에 파티션 키를 포기할 수 있었다."**
> 자료구조 선택 하나가 멱등성·전달 보장·파티셔닝까지 연쇄로 결정한다.
> README에 이 인과를 명시하면 설계가 일관됐다는 증거가 된다.

---

### 7.12 [언급만] Redis 클러스터에서의 fan-out

`timeline:{userId}`가 여러 노드에 흩어지면 파이프라이닝 효율이 떨어진다.
Hash Tag로 묶으면 파이프라이닝은 되지만 특정 노드에 쏠린다.

단일 Redis로도 프로젝트가 성립하므로 구현하지 않는다.
**"클러스터로 확장 시 이런 문제가 생긴다"고 README에 언급만 한다.**

---

### 7.13 [필수] Redis가 죽으면 정말 살아남는가 — 캐시 스탬피드

§1이 던진 네 번째 질문의 실제 답이다.

§5-7의 "키 없음 → Pull 폴백"은 **개별 캐시 미스의 답이지 전면 장애의 답이 아니다.**
Redis가 죽으면 전 사용자가 동시에 그 경로로 몰린다 —
Phase 1에서 가장 느렸던 바로 그 쿼리로. **DB가 즉사한다.**

폴백 경로를 살려두는 것과, 그 경로가 몰려도 버티는 것은 별개의 문제다.

**결정**

| 대응 | 구현 |
|---|---|
| 폴백 동시 실행 제한 | 세마포어 20개. `tryAcquire(500ms)` 실패 시 `503 + Retry-After: 1` |
| 중복 폴백 방지 | `SET timeline:lock:{userId} 1 NX EX 10` — 실패 시 짧게 대기 후 캐시 재조회 |
| 폴백 결과 축소 | 500개가 아니라 **최근 100개만** Pull (→ §7.6이 여기에 흡수된다) |
| negative caching | 부재/삭제 게시글은 tombstone을 TTL 60초로 저장 |
| Circuit Breaker | Resilience4j를 **DB 보호 방향**으로 — 폴백 실패율 초과 시 open |

**근거**

- 부분 결과를 지어내는 것보다 명시적으로 거절하는 편이 낫다.
  503과 `Retry-After`는 표준적이고, 클라이언트가 재시도 정책을 세울 수 있다
- 세마포어 20은 DB 커넥션 풀 크기와 연동해 정한다 —
  **폴백이 커넥션을 전부 먹으면 정상 경로까지 죽는다**
- negative caching이 없으면 §7.5의 대량 삭제 시나리오가 그대로 DB 부하로 증폭된다

**검증 (Phase 4′ 장애 시나리오)**
- 부하 주입 중 `redis-cli SHUTDOWN` → DB가 버티는지, 503 비율이 얼마인지, 복구 시간이 얼마인지
- Redis 복구 후 캐시가 자연히 다시 채워지는지
- 동일 사용자 동시 요청 100건에서 Pull 쿼리가 몇 번 실행되는지 (락이 동작하는지)

---

### 쟁점 우선순위

| 쟁점 | 구현 비용 | 서술 가치 | 판단 |
|---|---|---|---|
| 7.7 Read-your-own-write | 매우 낮음 | 높음 | **필수** |
| 7.8 언팔로우 처리 | 낮음 | 높음 | **필수** |
| 7.3 멱등성 | 낮음 | 높음 | **필수** |
| 7.13 캐시 스탬피드 | 낮음 | 높음 | **필수** |
| 7.4 카운터 SoT | 낮음 | 중간 | 필수 |
| 7.5 재조회 상한 | 매우 낮음 | 중간 | 필수 |
| 7.9 팔로우 백필 | 낮음 | 중간 | 권장 |
| 7.6 비활성 복귀 | 없음(7.13에 흡수) | 중간 | 자동 |
| 7.11 Kafka 파티셔닝 | 중간 | 높음 | Phase 3 시 자동 |
| 7.10 핫 키 2계층 캐시 | 중간 | 중간 | **제외** |
| 7.12 Redis 클러스터 | 높음 | 중간 | 언급만 |

7.7 / 7.8 / 7.9 / 7.13은 **Phase 2에 얹어도 일정에 거의 영향이 없으면서** 면접 대화를 크게 늘린다.
7.10 / 7.12는 실제로 시간을 잡아먹으므로 하지 않는다.

---

## 8. 단계별 로드맵

### 일정 구조 — 12주 시점에 완결된 산출물이 나오게 한다

| Phase | 내용 | 기간 | 누적 |
|---|---|---|---|
| 0 | 기반 구축 | 2주 | 2주 |
| 1 | 더미 데이터 + M0/M1 측정 | 3주 | 5주 |
| 2a | Push 기본 + §7.13 → M2/M3 측정 | 2주 | 7주 |
| 2b | 필수 쟁점 + 정합성 검증 → 재측정 | 2주 | 9주 |
| **4′** | **장애 시나리오 + README/리포트 완성** | 3주 | **12주 ← 제출 가능** |
| *3* | *Kafka + Hybrid + 임계치 비교 → M4 측정* | *3주* | *15주* |
| *4″* | *문서 갱신* | *1주* | *16주* |

**왜 문서화를 앞으로 당겼는가**

§12의 첫 번째 리스크는 "완주 실패"이고, 그 대응은 "Phase 2까지만 해도 충분하다"이다.
**그 판단이 실제로 작동하려면 일정 구조에 반영되어야 한다.**
README와 성능 리포트를 맨 끝에 두면, 중단했을 때 남는 것이 **코드뿐인 상태**가 된다.

문서화를 12주 안에 넣으면 그 시점에 **항상 제출 가능한 완결 산출물**이 존재하고,
Phase 3는 그 위에 얹는 보너스가 된다.

---

### Phase 0 — 기반 구축 (2주)

- [ ] 프로젝트 세팅, 패키지 구조 설계 (도메인별 분리)
- [ ] Docker Compose 오버레이 3종 (`dev` / `bench` / `async`) — §3
- [ ] Flyway 마이그레이션 · `ddl-auto: validate`
- [ ] 회원가입/로그인 (JWT + `refresh:{userId}`)
- [ ] **가입 시 self-follow 행 삽입** (→ §4.3)
- [ ] 게시글 CRUD(수정 제외), 팔로우 기능
- [ ] Snowflake ID 생성기 + **타임스탬프 주입형 팩토리를 더미 소스셋에 격리** (→ §4.2)
- [ ] Testcontainers 통합 테스트 환경
- [ ] GitHub Actions CI

**완료 조건**: 기능은 다 되지만 데이터가 없고 성능 개념도 없는 상태

---

### Phase 1 — 더미 데이터 & 첫 측정 (3주)

**이 단계가 프로젝트의 성패를 가른다.** 데이터 없이는 아무 문제도 발생하지 않는다.

#### 더미 데이터 분포 설계

원안의 "상위 0.1%는 팔로워 10만 이상"은 상위 100명만으로 1,000만 건이 되어
300만 예산의 3배를 초과한다. **산술적으로 성립하지 않으므로** 아래로 확정한다.

*팔로워 쪽 분포 (fan-out 부하를 결정)*

| 계층 | 인원 | 1인당 팔로워 | 소계 |
|---|---|---|---|
| S | 10 | 20,000 | 200,000 |
| A | 100 | 5,000 | 500,000 |
| B | 1,000 | 500 | 500,000 |
| C | 98,890 | ~18 | 1,800,000 |
| | | **합계** | **3,000,000** |

*팔로잉 쪽 분포 (Pull 조회 비용을 결정) — 측정 코호트를 여기서 만든다*

| 코호트 | 인원 | 팔로잉 수 | 소계 | 용도 |
|---|---|---|---|---|
| `heavy` | 1,000 | 500 | 500,000 | 팔로잉 수 영향 측정 |
| `normal` | 2,000 | 100 | 200,000 | 대표값 |
| `light` | 2,000 | 10 | 20,000 | 기본 성능 |
| 나머지 | 95,000 | ~24 | 2,280,000 | 배경 |

**부하 테스트는 위 5,000명 코호트에서만 사용자를 뽑는다.**
10만 명을 랜덤으로 치면 매 요청이 콜드 캐시라 Push의 효과가 측정되지 않는다.
실서비스는 활성 사용자가 반복 접속하므로 제한된 풀이 오히려 현실적이다.
그리고 랜덤 샘플링에서 팔로잉 500명이 뽑힐 확률은 1%라 **"팔로잉 500명 조회" 시나리오 자체가 성립하지 않는다.**

#### 작업

- [ ] 더미 데이터 생성 (JDBC Batch Insert — JPA로 넣으면 며칠 걸린다)
  - 사용자 10만 명
  - 팔로우 관계 300만 건 (위 분포)
  - 게시글 300만 건, **본문 평균 80B**
  - 시간 분포: 최근 7일 25% / 8~30일 35% / 31~180일 40%
  - 노드ID 0~15로 16스레드 병렬 생성 (ms당 4,096 시퀀스 한계 회피)
- [ ] `innodb_buffer_pool_size = 1G` 설정
- [ ] Pull 방식으로 타임라인 조회 구현
  ```sql
  SELECT p.* FROM posts p
  JOIN follows f ON p.author_id = f.followee_id
  WHERE f.follower_id = :userId
    AND p.id < :cursor
    AND p.is_deleted = false
  ORDER BY p.id DESC LIMIT 25
  ```
  > self-follow 행이 있으므로 이 쿼리로 **내 글도 함께 조회된다.**
  > `is_deleted` 조건을 처음부터 포함해 Phase 2와 결과 집합을 맞춘다.
- [ ] k6 시나리오 작성 (→ §9, **open model**)
- [ ] **M0 측정** (인덱스 없음) — 여기가 baseline이다
- [ ] `EXPLAIN ANALYZE`로 실행 계획 분석, 인덱스 설계
- [ ] **M1 측정** (복합 인덱스 적용)

**왜 게시글을 500만에서 300만으로 줄였는가**

총 데이터가 buffer pool 1GB를 넘으면 **측정치가 디스크 I/O 변동성에 지배당해
개선 폭을 증명한다는 목적 자체가 무너진다.** 규모보다 측정 안정성이 우선이다(§12).

**Phase 1 baseline은 데이터 크기가 아니라 동시성에서 나온다.**
팔로잉 500명이면 커서 이후 수천~1만 행을 읽어 정렬하는데, 이를 동시에 수백 건 처리하면
8코어로는 큐잉이 발생한다. 300만 건에서도 재현되며,
오히려 **병목이 I/O가 아니라 CPU/정렬이라 분석이 더 깨끗하다.**

> **하향 트리거**: 측정 중 buffer pool 히트율이 95% 미만이면 게시글을 200만으로 재조정하고
> 그 사실을 리포트에 기록한다.

**예상 결과**: p99가 수 초 단위로 나온다. 이게 정상이고, 이 수치가 있어야 개선이 의미를 갖는다.

**기록할 것**
- 팔로잉 수에 따른 응답 시간 변화 (`light` / `normal` / `heavy` 코호트)
- **M0 대비 M1의 개선 폭** — "인덱스만으로는 한계가 있었다"를 주장하려면 이 숫자가 필요하다
- 병목이 어디인지 (slow log + `EXPLAIN ANALYZE` + JFR)

---

### Phase 2a — Push 방식 전환 (2주)

- [ ] 게시글 작성 시 팔로워 타임라인에 fan-out (동기)
- [ ] Redis Sorted Set 타임라인 구현
- [ ] 2단계 조회 (ID 목록 → 본문 MGET)
- [ ] 캐시 미스 시 DB 폴백 + 캐시 워밍
- [ ] 타임라인 길이 제한 (500개)
- [ ] soft delete + 읽기 시점 필터링
- [ ] **§7.13 캐시 스탬피드 대응** (세마포어 / 락 / negative cache / Circuit Breaker)
- [ ] **M2(cold) / M3(warm) 측정**

**완료 조건**: "Pull → Push로 p99를 개선했다"는 **완결된 서사 하나가 성립한다.**
여기서 중단해도 포트폴리오가 된다.

---

### Phase 2b — 필수 쟁점 & 정합성 (2주)

- [ ] **Read-Your-Own-Write** (→ §7.7) — 본인 동기 ZADD, 지연 5초 주입 후 검증
- [ ] **언팔로우 A/B/C 비용 실측** (→ §7.8) — C 채택, 표를 README에
- [ ] **팔로우 백필 N=20, 인플루언서 제외** (→ §7.9)
- [ ] **재조회 상한 3회** (→ §7.5) — 삭제율 0~90% 구간 측정
- [ ] **카운터 정합성** (→ §7.4) — 동시 좋아요 유실 테스트
- [ ] **Pull/Push 동등성 검증** (→ §9.4) — CI에 편입
- [ ] **Virtual Thread fan-out 실험** — 플랫폼 스레드풀과 비교, pinning 측정
- [ ] **재측정** — 쟁점 구현이 성능에 준 영향 확인

**Virtual Thread 실험 범위**
fan-out 워커에만 적용한다. 웹 요청 처리 스레드(Tomcat)는 건드리지 않는다.
`-Djdk.tracePinnedThreads=full`로 pinning 발생 여부를 함께 측정한다 —
**"써봤다"가 아니라 "한계를 안다"가 되는 지점이다.**

**여기서 만나게 될 문제**
- 팔로워가 많은 계정이 글 쓰면 응답이 오래 걸린다 → Phase 3의 동기
- 캐시와 DB 데이터가 안 맞는 상황 → 동등성 검증이 잡아낸다
- Redis 메모리가 예상보다 빨리 찬다 → LRU 축출이 관측된다

---

### Phase 4′ — 안정화 & 문서화 (3주) ← **12주 시점의 완결 지점**

- [ ] 장애 시나리오 테스트 (Redis 중단 / DB 커넥션 고갈)
- [ ] Grafana 대시보드 구성
- [ ] Circuit Breaker 동작 검증
- [ ] **README 완성** ← 실질적인 최종 산출물
- [ ] 아키텍처 다이어그램
- [ ] 성능 리포트 정리 + **원시 k6 결과 커밋**
- [ ] Phase별 git 태그 (`v1-pull`, `v2-push`)

---

### Phase 3 — 비동기 처리 & Hybrid (3주) *[보너스]*

- [ ] fan-out을 Kafka로 분리 (글 작성 → 이벤트 발행 → 즉시 응답)
- [ ] 컨슈머 재시도 / DLQ 설계
  - 멱등 연산이므로 at-least-once로 충분하다는 논리 정리 (→ §7.3)
- [ ] **파티션 키 없이 500명 청크 분할 발행** (→ §7.11)
- [ ] 활성 사용자 필터링 (`active:users` 파이프라인 ZSCORE)
- [ ] **Hybrid 전략 구현**
  - 팔로워 5,000 미만 → Push
  - 5,000 이상(influencer) → Push 안 함
  - 조회 시: `내 타임라인 캐시` + `팔로우한 인플루언서의 최근 글` 머지
  - 머지 범위는 `postId > snowflakeOf(influencer_since)` (→ §7.2)
- [ ] **M4 측정**
- [ ] **임계치 3종(500 / 5,000 / 20,000) 비교 측정** (→ §7.2)

**설계 결정 기록 (면접 대비 핵심)**
- 임계치를 왜 5,000으로 잡았는가 — 그리고 팔로워 수만으로 정한 것의 한계는
- 머지 비용은 얼마나 드는가
- 인플루언서를 많이 팔로우한 사용자는 어떻게 되는가
- Kafka 컨슈머가 밀리면 타임라인은 어떻게 되는가

> **리소스 주의**: 이 Phase에서는 Kafka 자리를 만들기 위해 모니터링 스택을 내린다(§3).
> fan-out 지연은 Actuator와 애플리케이션 로그로 계측한다.

---

## 9. 성능 측정 계획

**이 절은 부록이 아니다.** 측정이 신뢰할 수 없으면 §7의 모든 결정이 근거를 잃는다.

### 9.1 부하 모델 — open model을 쓴다

```javascript
export const options = {
  scenarios: {
    // ① 포화점 탐색: RPS를 올리며 p99가 꺾이는 지점을 찾는다
    saturation: {
      executor: 'ramping-arrival-rate',
      startRate: 100, timeUnit: '1s',
      preAllocatedVUs: 200, maxVUs: 3000,
      stages: [
        { duration: '1m', target: 250 },
        { duration: '1m', target: 500 },
        { duration: '1m', target: 1000 },
        { duration: '1m', target: 2000 },
        { duration: '1m', target: 3000 },
      ],
    },
    // ② SLO 검증: 목표 RPS를 고정 주입한다
    slo: {
      executor: 'constant-arrival-rate',
      rate: 1000, timeUnit: '1s',
      duration: '5m',
      preAllocatedVUs: 400, maxVUs: 2000,
      startTime: '2m',        // JVM 워밍업 이후 계측
    },
  },
  thresholds: {
    http_req_duration: ['p(99)<200'],
    http_req_failed: ['rate<0.01'],
    dropped_iterations: ['count==0'],   // 주입량을 못 채우면 측정 무효
  },
};
```

> 위 saturation 사다리(100→3,000)는 M3(warm) 기준이다. M0/M1처럼 예상 처리량이 낮은
> 측정 지점에서는 전 구간 `dropped_iterations > 0`이 되어 측정 자체가 무효가 되므로,
> **구조는 동일하게 유지하되 사다리 수치만 예상 포화점에 맞춰 하향 조정한다**
> (예: M0/M1은 10→250 RPS 사다리에서 시작).

**왜 `ramping-vus`를 버렸는가**

`ramping-vus`는 closed model이다. VU가 "요청 → 응답 대기 → 다음 요청"을 돌기 때문에
**서버가 느려지면 부하가 자동으로 줄어든다.**
그 결과 지연이 과소평가되고(**coordinated omission**),
"TPS 1,000"이 서버의 한계인지 내가 주입한 양인지 구분할 수 없다.

이 함정은 숫자로 드러난다. 예컨대 `p50 31ms / p99 178ms / TPS 1,240 (VU 500)`이라는 결과는
Little's Law(동시성 = 처리량 × 평균 응답시간)로 평균 응답 403ms를 함의하는데,
p99 178ms와 모순이다. **VU가 놀고 있었다는 뜻이다.**

`dropped_iterations`가 0이 아니면 주입 목표를 못 채운 것이므로 **그 측정은 폐기한다.**

> **이 전환 과정 자체를 README에 남긴다.**
> closed model로 쟀다가 open model로 다시 잰 이야기는 부하 테스트를 이해했다는 신호다.

### 9.2 측정 지점 — 변수를 하나씩 통제한다

| ID | 조건 | 목적 |
|---|---|---|
| **M0** | Pull, 인덱스 없음 | 최악 baseline |
| **M1** | Pull + 복합 인덱스 | **인덱스만으로 얼마나 개선되는가** |
| **M2** | Push, cold 캐시 | 폴백 경로 비용 |
| **M3** | Push, warm 캐시 | 본 성능 |
| **M4** | Hybrid (Phase 3) | 머지 비용 |

Phase 1 → 2 사이에 인덱스도, 캐시도, 조회 경로도 한꺼번에 바뀐다.
쪼개지 않으면 **무엇이 개선을 만들었는지 귀속할 수 없다.**

특히 **M1이 "인덱스만으로는 한계가 있었다"는 주장의 유일한 증거다.**
이 숫자가 없으면 §10의 서술 원칙이 요구하는 "실패한 시도"가 근거 없는 문장이 된다.

### 9.3 측정 프로토콜

**고정 조건**
- 매 측정 전 MySQL Docker 볼륨 스냅샷 복원 (데이터 동일성 보장)
- 캐시 상태: cold는 `FLUSHALL` 직후, warm은 5분 워밍 후 — **두 조건 모두 기록**
- JVM 워밍업 2분 후 계측 시작 (`startTime: '2m'`)
- **3회 반복, 중앙값 보고, 최대-최소 편차 병기**
- 사용자는 §8 Phase 1의 5,000명 코호트에서만 추출, 시나리오별로 코호트 고정
- **JWT는 `setup()`에서 사전 발급해 VU가 재사용한다** —
  매 요청 로그인하면 BCrypt가 병목이 되어 타임라인이 아니라 인증 비용을 재게 된다

**서버 사이드 계측 병행**
Micrometer `@Timed`로 DB / Redis / 직렬화 구간을 분해한다.
```
클라이언트 지연 − 서버 내부 지연 = 큐잉 시간
```
이 차이가 포화의 증거이고, "어디가 병목인가"에 답하는 근거다.

**부하 생성기 격리 — 한계를 명시한다**
k6는 호스트에서 실행되어 8코어를 애플리케이션과 공유한다. **완전한 격리는 불가능하다.**
- 측정 중 k6 프로세스의 CPU 사용률을 함께 기록해 경합 여부를 판단 가능하게 한다
- 이 한계를 리포트에 명시한다 — **숨기는 것보다 밝히는 편이 신뢰를 얻는다**

### 9.4 정합성 검증 — 성능만 재고 정확성을 안 재면 안 된다

```
동등성 테스트: 임의 사용자 100명에 대해
  Pull 쿼리 결과 20건  ==  Push 타임라인 조회 결과 20건   (postId 시퀀스 일치)
```

- 통합 테스트로 **CI에서 상시 실행** (Phase 2 이후 회귀 방지)
- fan-out을 강제 중단한 뒤 재처리해도 동등성이 유지되는지 → **§7.3 멱등성의 실증**
- 부하 테스트 직후 샘플링해 **불일치율(%)을 리포트 지표로 기록**

> 이 검증이 없으면 "캐시-DB 정합성을 관리했다"(§10)는 주장에 증거가 없다.
> 틀린 답을 빠르게 주는 시스템을 만들고도 모르는 상태가 된다.

### 9.5 측정 시나리오

| 시나리오 | 코호트 | 확인할 것 |
|---|---|---|
| 일반 사용자 타임라인 조회 | `normal` | 기본 성능 |
| 팔로잉 500명 사용자 조회 | `heavy` | 팔로잉 수 영향 |
| 인플루언서 글 작성 | S 계층 | fan-out 지연 (단독 시나리오) |
| 조회 98% + 작성 2% 혼합 | 전체 | 실제 트래픽 근사 (→ §7.1) |
| 캐시 전체 삭제 후 조회 | 전체 | 폴백 동작 (M2) |
| Redis 중단 중 조회 | 전체 | **§7.13 스탬피드 방어** |
| 삭제율 0~90% 타임라인 | `normal` | **§7.5 재조회 상한** |

### 9.6 재현 가능성

면접관은 "정말 그 규모로 쟀나"를 검증할 수 없다. 수치는 얼마든지 쓸 수 있으므로
**검증 가능성이 곧 신뢰도다.**

- [ ] 더미 생성 + 부하 테스트 + 리포트 생성을 **한 명령으로** (`make bench-m3`)
- [ ] **k6 원시 결과(JSON/HTML summary)를 저장소에 커밋** — 가공된 표만 있으면 신뢰가 안 간다
- [ ] 결과를 고정 경로에 누적: `docs/perf/m0.md`, `m1.md`, …
- [ ] 측정 환경(칩·코어·메모리·컨테이너 리소스 제한)을 리포트마다 고정 서식으로 기록
- [ ] **Phase별 git 태그** (`v1-pull`, `v2-push`, `v3-hybrid`)
      — 같은 기능을 3번 구현하는 프로젝트이므로 태그가 곧 서사 구조다

### 9.7 기록 템플릿

```markdown
## M3 — Push, warm 캐시

### 환경
- Apple M1 8core / RAM 8GB
- MySQL 8.0 (Docker 2G, buffer pool 1G) / Redis 7 (Docker 1.5G, maxmemory 1G)
- 데이터: 사용자 10만, 게시글 300만, 팔로우 300만
- k6: constant-arrival-rate 1,000 RPS, 5분, `heavy` 코호트
- 3회 반복 중앙값 / dropped_iterations = 0

### 결과
| 지표 | M0 (Pull) | M1 (Pull+인덱스) | M3 (Push) |
|---|---|---|---|
| p50 | | | |
| p99 | | | |
| 최대 RPS (p99<200ms) | | | |
| 불일치율 | - | - | 0% |

### 원인 분석
(EXPLAIN ANALYZE 실측 행수, slow log, JFR 플레임그래프 근거)

### M1이 충분하지 않았던 이유
(인덱스로 개선된 폭과 남은 병목)

### 남은 문제
(→ 다음 Phase의 동기)
```

---

## 10. README 구성 (가장 중요)

**면접관은 코드를 거의 안 본다. README를 본다.**

```markdown
# 타임라인 서비스

## 한 줄 소개
300만 건 데이터 환경에서 타임라인 조회 p99를 X초 → Yms로 개선한 프로젝트

## 이 프로젝트가 다른 캐시 적용 사례와 다른 점
- 같은 기능을 Pull / Push / Hybrid로 세 번 구현하고 각각 측정했다
- 왜 다음 단계로 갔는지를 수치와 EXPLAIN ANALYZE로 남겼다
- 인덱스만으로 해결하려다 실패한 지점(M1)의 수치도 함께 적었다
- 해결하지 못한 한계(임계치를 팔로워 수로만 정한 것 등)를 숨기지 않았다

## 아키텍처
[다이어그램]

## 기술적 도전과 해결

### 1. 팔로잉이 많을수록 타임라인이 느려지는 문제
- 상황: 팔로잉 500명 기준 p99 X초
- 원인: JOIN 후 filesort, N만 행 정렬 (EXPLAIN ANALYZE 실측)
- 시도한 것: 복합 인덱스 → M1에서 Z% 개선에 그침
- 해결: Push 방식(fan-out on write) 전환
- 결과: p99 Yms
- 트레이드오프: 쓰기 비용 증가, Redis 메모리 필요

### 2. Redis가 죽으면 Pull 폴백이 DB를 죽인다
- 상황: 폴백 경로는 살려뒀지만 전면 장애 시 전 사용자가 동시에 몰림
- 원인: 캐시 스탬피드. "폴백이 있다"와 "폴백이 버틴다"는 다른 문제
- 해결: 동시 실행 제한 + 중복 폴백 락 + negative cache + DB 보호 Circuit Breaker
- 검증: 부하 중 Redis SHUTDOWN 시 DB 생존 / 503 비율 / 복구 시간

### 3. 비동기 처리 후 내 글이 보이지 않는 문제
- 상황: 작성 직후 타임라인에 내 글이 없음
- 원인: fan-out 지연. read-your-own-write consistency 위반
- 해결: 본인 타임라인만 동기 처리로 분리

### 4. 캐시가 맞는지 어떻게 아는가
- Pull 결과와 Push 결과의 동등성을 CI에서 상시 검증
- fan-out 중단 후 재처리해도 동등한지 확인 → 멱등성의 실증
- 불일치율을 성능 지표와 나란히 기록

### 5. 자료구조 선택 하나가 어디까지 규정하는가
Sorted Set을 택한 이유는 커서 페이지네이션이 아니라 멱등성이었고,
그 덕분에 Kafka at-least-once로 충분했으며,
순서 보장이 불필요해져 파티션 키까지 포기할 수 있었다 (핫 파티션 원천 차단).

## 측정 방법
- open model(constant-arrival-rate)을 쓴 이유와 closed model의 함정
- 변수 통제: M0~M4로 나눠 측정한 이유
- 부하 생성기를 분리하지 못한 한계와 그 영향

## 이 문제의 일반화

fan-out은 SNS 고유의 문제가 아니라,
파생 데이터의 계산 시점을 쓰기와 읽기 중 어디에 둘 것인가의 문제다.

- 메시징: 그룹 채팅 메시지 배포 (대규모 채널 = 셀럽 문제)
- 권한: 폴더 권한 변경의 하위 전파 (틀리면 보안 사고)
- 커머스: 대량 쿠폰 발급, 재고의 채널 동기화
- 인프라: DB 읽기 복제, CDN 전파, CQRS Read Model, Materialized View

공통 난제는 fan-out 자체가 아니라 **분포의 쏠림**이며,
Hybrid 전략은 꼬리(tail)를 몸통과 분리 처리하는 일반해다.

## 의도적으로 하지 않은 것
- MSA: 단일 개발자 프로젝트에서 서비스 분리는 운영 복잡도만 증가
- 프론트엔드: 백엔드 성능 최적화에 집중하기 위해
- QueryDSL: 쿼리가 전부 정적이라 도입 이유를 설명할 수 없다
- 2계층 로컬 캐시: 정합성 복잡도 대비 얻는 게 적다 (문제는 인지하고 있다)

## 실행 방법
docker compose up
make bench-m3
```

### 서술 원칙

**문제 → 원인 분석 → 시도 → 해결 → 결과 → 남은 한계**

- "Redis를 사용했습니다" (X)
- "JOIN + filesort로 p99 X초가 나왔고, 인덱스로는 Z%밖에 줄지 않아 사전 계산 방식으로 전환했습니다" (O)

**실패한 시도도 적는다.** 인덱스만으로 해결하려다 안 됐던 과정이 오히려 사고 과정을 보여준다.
그래서 M1을 별도 측정 지점으로 잡았다(§9.2) — **실패를 적으려면 실패의 수치가 있어야 한다.**

---

## 11. 면접 예상 질문

이 질문들에 답할 수 있으면 프로젝트가 완성된 것이다.
**각 질문 옆의 근거 위치가 비어 있으면 그게 곧 남은 할 일이다.**

**설계**

| 질문 | 근거 |
|---|---|
| Push와 Pull 중 Push를 택한 이유는? 읽기:쓰기가 몇 대 몇이면 뒤집히는가? | §7.1 + 혼합 시나리오 측정 |
| Hybrid 임계치를 5,000으로 정한 근거는? 팔로워 수만으로 정하는 게 타당한가? | §7.2 + 임계치 3종 비교 |
| Snowflake ID를 쓴 이유는? AUTO_INCREMENT의 문제는? | §4.2 |
| 커서 페이지네이션을 쓴 이유는? 한계는? | §6 |
| Sorted Set 대신 List를 쓰면 무엇이 깨지는가? | §5-3, §7.3, §7.11 |
| 인플루언서로 승격되는 순간 글이 중복되거나 누락되지 않는가? | §7.2 (`influencer_since`) |

**장애 대응**

| 질문 | 근거 |
|---|---|
| Redis가 죽으면 어떻게 되는가? | **§7.13** + 장애 시나리오 테스트 |
| 폴백이 있는데 왜 DB가 죽는가? 어떻게 막았는가? | §7.13 (세마포어 / 락) |
| Kafka 컨슈머가 밀리면 사용자에게 어떻게 보이는가? | Phase 3 측정 |
| fan-out 도중 서버가 죽으면 일부 팔로워만 글을 받는데? | §7.3 + 중단·재처리 테스트 |
| 재처리 시 중복이 생기지 않는다고 어떻게 보장하는가? | §7.3 + §9.4 동등성 검증 |

**정합성**

| 질문 | 근거 |
|---|---|
| 좋아요 카운터가 실제 값과 어긋나면? 무엇이 진실인가? | §7.4 (DB. 1시간 stale 허용) |
| 글 삭제 시 이미 뿌려진 타임라인 캐시는? | §5-1, §7.5 |
| 사용자가 자기 글 500개를 한꺼번에 지우면? | §7.5 (삭제율 73%까지 보장) |
| 팔로우를 끊으면 이미 들어간 글은 사라져야 하는가? | §7.8 (C 채택 + 차단이면 뒤집힘) |
| 비동기 처리인데 내가 쓴 글은 왜 즉시 보이는가? | §7.7 |
| **캐시가 맞다는 걸 어떻게 아는가?** | **§9.4 동등성 검증 + 불일치율 지표** |

**측정**

| 질문 | 근거 |
|---|---|
| 그 수치를 어떻게 쟀는가? | §9.1~9.3 |
| VU를 늘리는 방식(closed model)의 문제는? | §9.1 (coordinated omission) |
| 개선이 인덱스 때문인지 캐시 때문인지 어떻게 구분했는가? | §9.2 (M0~M4) |
| 부하 생성기가 같은 머신에 있는데 신뢰할 수 있는가? | §9.3 (한계 명시 + CPU 사용률 기록) |

**확장**

| 질문 | 근거 |
|---|---|
| 사용자가 100배가 되면 어디가 먼저 터지는가? | §5-4 스케일 계산 |
| Redis 메모리가 부족해지면? | §5-5 (LRU 축출 → Pull 복구, 실측) |
| 바이럴 게시글 하나에 요청이 몰리면? | §7.10 (미구현, 대응 방향 서술) |
| Redis 클러스터로 가면 무엇이 바뀌는가? | §7.12 |
| 이 구조에서 랭킹 기반 피드로 바꾸려면? | (미작성) |

**일반화 (도메인이 다른 회사 면접에서)**
- 이 경험을 저희 도메인에 어떻게 적용할 수 있나?
  → fan-out은 파생 데이터의 계산 시점 문제이며, 메시징·권한 전파·대량 쿠폰 발급·
     재고 동기화·CQRS Read Model 갱신이 모두 같은 구조임을 설명할 수 있어야 한다.
     핵심 난제는 fan-out이 아니라 **분포의 쏠림**이라는 점까지 연결한다.

---

## 12. 리스크 관리

| 리스크 | 대응 |
|---|---|
| **완주 실패 (가장 큰 위험)** | 12주 시점(Phase 4′)에 README까지 완결되도록 일정을 짰다. Phase 3은 보너스 |
| **로컬 리소스 (8GB)** | 컨테이너 프로파일 3종으로 분리. Phase 3에서는 모니터링을 내린다 |
| **측정치가 리소스 경합에 오염** | buffer pool 안에 들어가는 데이터 규모 선택. 히트율 95% 미만 시 200만으로 하향 |
| 기능 추가 욕심 | 댓글, 해시태그, 검색, DM, 차단, 게시글 수정 전부 금지 |
| 측정 없이 개발만 함 | 각 Phase 끝에 측정 없이 다음으로 넘어가지 않는다. M0~M4가 체크포인트 |
| **측정을 했는데 신뢰할 수 없음** | open model + 변수 통제 + 3회 반복 + 원시 결과 커밋 (§9) |
| **빠른데 틀림** | 동등성 검증을 CI에 편입. 불일치율을 성능과 나란히 기록 (§9.4) |
| 쟁점 과다 착수 | §7의 [필수] 4개(7.3/7.7/7.8/7.13)를 우선. 7.10/7.12는 하지 않는다 |
| 답 없이 구현만 함 | §11의 근거 열이 비어 있으면 그게 남은 할 일이다 |

---

## 13. 참고 자료

- Twitter Timelines at Scale (Raffi Krikorian) — fan-out 전략의 원전
- Designing Data-Intensive Applications, 1장 — Twitter 사례와 읽기/쓰기 비율 실측치
- Redis Sorted Set 공식 문서 / `MEMORY USAGE`, listpack↔skiplist 전환
- Real MySQL 8.0 — 인덱스 / 실행 계획
- k6 executors 문서 — open vs closed model, coordinated omission

---

## 부록 A: 첫 주에 할 일

1. GitHub 저장소 생성, README 뼈대 작성
2. Spring Boot 프로젝트 생성 (Java 21, JPA, Redis, Security)
3. `compose.yml` 작성 (MySQL + Redis, 메모리 제한 포함)
4. **Flyway 마이그레이션으로** users / posts / follows / likes 스키마 작성
5. Snowflake ID 생성기 + 타임스탬프 주입형 팩토리(더미 소스셋 격리)
6. 회원가입 API 하나 완성 — **self-follow 행 삽입 포함** + Testcontainers 통합 테스트 1개

**6번까지 되면 나머지는 반복이다. 첫 주에 여기까지 가는 것이 목표.**

---

## 부록 B: 착수 전 확정된 결정 요약

원안에서 바뀐 항목과 그 이유를 한눈에 본다.

| 항목 | 원안 | 확정 | 이유 |
|---|---|---|---|
| 게시글 수 | 500만 | **300만** | buffer pool 1G 안에 들어가야 측정이 안정된다 |
| 본문 길이 | 미정 | **평균 80B** | 위와 동일. 실제 트윗 길이와도 부합 |
| 팔로워 분포 | 상위 0.1%가 10만+ | **S/A/B/C 4계층** | 원안은 300만 예산의 3배가 되어 성립 불가 |
| Hybrid 임계치 | 1만/10만/50만 | **5백/5천/2만** (기본 5천) | 10만 팔로워는 데이터셋에서 불가능. 하한 5백은 B 계층이 걸리는 값 (1천이면 5천과 동일 조건) |
| 인플루언서 전환 | 미정 | **단방향 승격 + `influencer_since`** | 강등은 소급 백필 문제를 만든다 |
| 타임라인 상한 | 800 | **500** | Redis 1G 제약. 25페이지면 충분 |
| Redis 메모리 | 6.4GB 추산 | **maxmemory 1G + LRU** | 8GB 시스템. LRU 축출이 그 자체로 실험이 된다 |
| `followers:{userId}` | Set 캐시 | **폐기** | 무효화 없는 TTL은 신규 팔로워 유실을 만든다 |
| `like:count:{postId}` | String 카운터 | **폐기** | 카운터의 진실을 DB로 확정 |
| 카운터 SoT | 미결정 | **DB (1시간 stale 허용)** | 좋아요는 병목이 아니다 |
| 자기 글 포함 | 불일치 | **self-follow 행 삽입** | Pull/Push 결과 집합을 맞춰야 비교가 성립 |
| 읽기:쓰기 | 미정 | **50:1** | DDIA 실측 65:1보다 보수적으로 |
| 언팔로우 | 미결정 | **C(방치)** | B는 읽기 경로에 부하를 더한다 — 목적과 정반대 |
| 백필 N | 미정 | **20, 인플루언서 제외** | 첫 페이지 크기. 제외로 도배 문제가 자동 해소 |
| 재조회 상한 | 미정 | **3회** | 삭제율 73%까지 20개 보장 |
| 비활성 복귀 | 미정 | **§7.13에 흡수** | 캐시 미스의 특수 케이스일 뿐 |
| Kafka 파티션 키 | 미정 | **없음 + 500명 청크** | ZADD가 순서 무관이라 포기해도 잃는 게 없다 |
| 부하 모델 | `ramping-vus` | **open model** | closed model은 지연을 과소평가한다 |
| 측정 지점 | 단일 | **M0~M4** | 개선의 귀속을 위해 |
| 정합성 검증 | 없음 | **Pull==Push 동등성** | 성능만 재고 정확성을 안 재면 안 된다 |
| QueryDSL | 채택 | **제외** | 쿼리가 전부 정적이다 |
| Flyway | 없음 | **채택** | 인덱스 변경 이력이 곧 실패한 시도의 기록 |
| Virtual Thread | 근거만 있음 | **Phase 2b fan-out 한정 실험** | 근거와 계획이 따로 놀았다 |
| 게시글 수정 | 논거로만 등장 | **미구현 확정** | 실재하지 않는 기능에 기댄 논거였다 |
| §7.13 | 없음 | **신설 [필수]** | "Redis 죽어도 산다"의 답이 원안엔 없었다 |
| 문서화 시점 | Phase 4(마지막) | **12주 안(Phase 4′)** | 중단 시 코드만 남는 구조를 피한다 |

*아래는 로드맵(docs/roadmap/) 작성 과정에서 추가 확정된 결정이다.*

| 항목 | 확정 | 이유 |
|---|---|---|
| Access Token TTL | **30분** (Refresh 14일) | 표준적 값. k6는 setup() 사전 발급이라 측정에 무영향 |
| Snowflake epoch / nodeId | **2025-01-01T00:00:00Z / 프로덕션 nodeId 0** | 더미 백데이팅 180일 커버. epoch은 한 번 정하면 불변 |
| 자기 자신 follow/unfollow API | **400 응답** | 시스템 self-follow 행(§4.3)을 API로 건드릴 수 없게 보호 |
| V1 인덱스 범위 | **posts 조회용 보조 인덱스 제외** | 인덱스는 M0 분석의 산출물이다 (§4.1 주석, §9.2) |
| 더미 인플루언서 승격 | **일괄 배치, `influencer_since`는 더미 게시글 시간창 이전으로 백데이팅** | 승격 시각을 현재로 잡으면 기존 글 전부가 머지 범위 밖이라 M4의 머지 비용이 공동화된다. 캐시 중복은 머지 dedup(§7.2)으로 흡수 |
| k6 saturation 사다리 | **측정 지점별 하향 조정 허용 (구조 동일)** | M0/M1은 낮은 사다리가 아니면 전 구간 dropped로 무효 (§9.1) |

*아래는 Phase 0 구현 과정에서 추가 확정된 결정이다.*

| 항목 | 확정 | 이유 |
|---|---|---|
| Spring Boot 버전 | **3.5.16** | start.spring.io가 4.x만 생성(3.x 거부)해 스켈레톤만 받고 3.x 최신 안정으로 재작성. 프로젝트 규칙(Boot 3.x 고정)이 우선 |
| posts의 FK 부산물 인덱스 | **FK 유지 — `fk_posts_author`(author_id 단일) 허용** | InnoDB는 FK 자식 컬럼 인덱스를 강제한다(제거 시 ERROR 1553). 따라서 M0의 "인덱스 없음"은 "**Pull 조회용 복합 인덱스 없음**"을 뜻하며, M1의 개선 귀속은 "복합 인덱스의 filesort 제거 효과"로 서술한다 (V1 주석에 해석 지침) |
| Redis persistence | **off (`--save ""`)** | 캐시 전용 — 키가 없으면 Pull 폴백으로 복구되는 설계(§5-7)라 불필요하고, fork 기반 스냅샷은 측정 중 latency spike로 수치를 오염시킨다 |
| DATETIME 정밀도 | **전 컬럼 DATETIME(6)** | Hibernate 6 매핑 기본(datetime(6))과 문자 그대로 일치. 정밀도 0은 반올림으로 `created_at`이 최대 +0.5초 밀려 Snowflake id/시간 순서 대조(§4.2)에 노이즈가 된다 |
| Snowflake 시계 역행 | **5ms 이하 대기 흡수 / 초과 시 즉시 실패(IllegalStateException)** | NTP slew는 스핀 대기로 흡수. step 보정을 대기로 버티면 락을 쥔 채 수 분 멈춘다. lastTimestamp 재사용 발급은 "ID = 시각" 전제를 소리 없이 깨므로 제외 |
| JWT 라이브러리 | **jjwt 0.12.x (api / impl / jackson 3분할)** | 컴파일 의존을 `jjwt-api`에만 걸면 애플리케이션 코드가 구현 패키지를 참조할 수 없다. Spring Security OAuth2 리소스 서버(Nimbus)는 인가 서버·JWK를 전제하는 스택이라 자체 발급 한 곳뿐인 이 구성에는 과하다 |
| 인증 principal | **userId(`Long`) + `@AuthenticationPrincipal Long`** | 토큰의 sub가 곧 userId다. `UserDetails`를 끼우면 요청마다 사용자 조회가 붙는데, 조회 98%(§9.1) 부하에서 그건 인증이 아니라 부하다. 커스텀 리졸버도 불필요 |
| 로그인 실패 응답 | **401 단일 코드 `LOGIN_FAILED`** — 사용자 부재/비밀번호 불일치 미구분 | 구분해서 응답하면 로그인 API가 username 존재 여부 확인 도구가 된다. 통합 테스트가 두 응답의 본문 동일성까지 검사한다 |
| 토큰 `jti` 클레임 | **UUID 부여** | `iat`·`exp`가 초 단위라 같은 초의 재로그인이 바이트 단위로 동일한 토큰을 낸다. 그러면 "재로그인이 이전 Refresh를 무효화한다"(§5)가 그 순간만 조용히 성립하지 않는다 |

*아래는 Phase 1 구현 과정에서 추가 확정된 결정이다.*

| 항목 | 확정 | 이유 |
|---|---|---|
| `timeline` 패키지의 도메인 경계 | **읽기 전용 특례 — `posts`·`follows`를 native SQL로 직접 조회한다.** 단, `Post`/`Follow` 엔티티도 두 도메인의 Repository도 참조하지 않는다(JdbcTemplate 전용). 쓰기는 특례에서 제외 | §8 Phase 1이 규정한 Pull 쿼리가 `posts JOIN follows` **한 문장**이고 그 문장 자체가 M0/M1의 측정 대상이다. FollowService→PostService로 쪼개면 쿼리가 둘이 되어 재려던 대상이 사라진다. 대신 자바 타입 결합을 0으로 묶어, 경계가 import 목록으로 확인되게 한다 |
| Pull 조회 구현 수단 | **`NamedParameterJdbcTemplate` + SQL 문자열 리터럴** (JPQL·QueryDSL·JPA native query 모두 제외) | 리포트의 `EXPLAIN ANALYZE` 대상과 서버가 보내는 SQL이 문자 그대로 같아야 분석이 코드에 대한 분석이 된다. JPQL은 방언이 SQL을 다시 쓰고, JPA native query는 `@Entity`에 묶인 리포지터리를 요구해 위 경계와 충돌한다. named parameter를 쓰는 것도 §8의 `:userId`/`:cursor` 표기를 그대로 살리기 위해서다 |
| `GET /timeline`의 `size` | **기본 20 · 상한 20(초과는 절삭, 400 아님)** | 조회 SQL의 `LIMIT 25`는 "25개 조회 → 삭제 필터 → 20개 반환"(§5-2)에서 온 고정 상수라 페이지 크기를 따라 커지지 않는다. 21 이상을 허용하면 `hasNext` 판정 여유가 4건 이하로 줄고, Phase 2a에서 그 여유가 삭제분에 먹히는 순간 "다음 페이지가 있는데 없다"고 답한다 |
| 서버 사이드 계측 방식 | **`Timer.builder(...).publishPercentileHistogram()` 명시 등록** (`@Timed` + `TimedAspect` 대신). 타이머는 `timeline.request` / `timeline.pull.query` 2종 | 히스토그램을 그 2종에만 켜기 위해서다. 전역 설정(`management.metrics.distribution.*`)으로 켜면 `http.server.requests`까지 버킷 시계열이 붙어 스크레이프 비용을 측정 중인 프로세스가 문다. AOP 프록시 한 겹을 측정 경로에서 빼는 효과도 있다 |
| posts 채택 인덱스 (P1-13) | **후보 B `(author_id, is_deleted, id DESC)`** — V3 상태가 최종, V4 불요 | A/B 실측 동률(heavy p50 ±10ms) 속에서 B는 is_deleted 필터를 인덱스 조건으로 흡수해 Filter 단계가 사라진다(EXPLAIN ANALYZE). soft delete가 실제로 쌓이는 Phase 2a 이후 이 차이가 커진다. 두 후보 모두 Sort는 못 없앴다 — JOIN이 follows 주도인 한 전역 정렬이 남는 것이 "인덱스만으로는 한계"의 구조적 근거 |
| InnoDB FK 인덱스 재바인딩 | (실측 기록) V2가 author_id 선행 인덱스를 만들면 자동 생성 인덱스 `fk_posts_author`는 소멸하고 FK가 재바인딩된다 → 이후 그 인덱스를 지우려면 대체 인덱스 선행 생성 필수 | V3 최초안(DROP 선행)이 ERROR 1553으로 실패한 실측. V2→V3 이력과 주석에 과정 보존 |
